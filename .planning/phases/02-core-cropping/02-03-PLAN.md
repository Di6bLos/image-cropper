---
phase: "02"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - app/pages/index.vue
  - app/components/CropWorkspace.vue
  - app/assets/sass/_variables.scss
  - app/assets/sass/main.scss
autonomous: true
requirements:
  - PRVW-01
  - PRVW-02
  - PRVW-03
  - CROP-01
  - CROP-02
  - CROP-03
  - CROP-04
  - CROP-05
  - CROP-06
  - RATIO-02
  - UIUX-01
  - UIUX-02
must_haves:
  truths:
    - "App renders a two-column layout with image list sidebar and crop workspace main area"
    - "System dark mode preference is detected and colors adapt accordingly"
    - "Layout stacks vertically on narrow screens (mobile)"
    - "Crop workspace displays image with overlay when image is selected"
    - "Crop window constrains to selected ratio when ratio preset changes"
  artifacts:
    - path: "app/assets/sass/_variables.scss"
      provides: "CSS variables for light/dark mode, crop-specific variables"
      min_lines: 50
    - path: "app/assets/sass/main.scss"
      provides: "Full-height layout, responsive grid, dark mode transitions"
      min_lines: 80
    - path: "app/components/CropWorkspace.vue"
      provides: "Image display + CropOverlay + RatioControls integration"
      min_lines: 100
      exports: ["default"]
    - path: "app/pages/index.vue"
      provides: "Two-column layout, responsive breakpoint, empty state"
      min_lines: 60
  key_links:
    - from: "app/pages/index.vue"
      to: "app/components/ImageList.vue"
      via: "sidebar column"
      pattern: "ImageList"
    - from: "app/pages/index.vue"
      to: "app/components/CropWorkspace.vue"
      via: "main column"
      pattern: "CropWorkspace"
    - from: "app/components/CropWorkspace.vue"
      to: "app/stores/useImageStore.ts"
      via: "watches selectedImage"
      pattern: "selectedImage"
    - from: "app/components/CropWorkspace.vue"
      to: "app/stores/useCropStore.ts"
      via: "watches effectiveRatio"
      pattern: "effectiveRatio"
    - from: "app/assets/sass/_variables.scss"
      to: "app/assets/sass/main.scss"
      via: "imported at top"
      pattern: "@use.*_variables"
---

<objective>
Create the main page layout that integrates all components into a working crop workspace, with responsive CSS and dark mode support.
</objective>

<context>
@.planning/phases/02-core-cropping/02-02-PLAN.md
@.planning/phases/02-core-cropping/02-CONTEXT.md
@app/components/DropZone.vue
@app/components/ImageList.vue
@app/components/CropOverlay.vue
@app/components/RatioControls.vue
@app/assets/sass/main.scss

Key layout:
- Main page has two-column layout: ImageList (left sidebar) + CropWorkspace (right main area)
- ImageList: fixed width (~280px), scrollable, contains thumbnails
- CropWorkspace: flexible, contains image + crop overlay + ratio controls
- Responsive: stack vertically on narrow screens (mobile)
- Dark mode: use CSS variables from main.scss, extend with crop-specific vars
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _variables.scss with dark mode and crop variables</name>
  <files>app/assets/sass/_variables.scss</files>
  <action>
Create app/assets/sass/_variables.scss with:

1. Light mode CSS variables:
   - --bg-primary: #ffffff
   - --bg-secondary: #f5f5f5
   - --bg-tertiary: #ebebeb
   - --text-primary: #1a1a1a
   - --text-secondary: #666666
   - --accent: #3b82f6
   - --accent-hover: #2563eb
   - --border: #e0e0e0
   - --overlay-opacity: 0.65 (D-07: 60-70% range)
   - --crop-handle-size: 8px (D-06)
   - --crop-border-width: 2px (CROP-05)
   - --crop-border-color: white

2. Dark mode overrides (prefers-color-scheme: dark):
   - --bg-primary: #1a1a1a
   - --bg-secondary: #2d2d2d
   - --bg-tertiary: #3d3d3d
   - --text-primary: #f5f5f5
   - --text-secondary: #a0a0a0
   - --accent: #60a5fa
   - --accent-hover: #3b82f6
   - --border: #404040

3. Import this file in main.scss before existing rules

Per UIUX-01: Dark mode support using CSS variables.
</action>
  <verify>
    <automated>grep -c "prefers-color-scheme" app/assets/sass/_variables.scss</automated>
  </verify>
  <done>_variables.scss created with: light/dark mode CSS variables, crop-specific variables (overlay opacity, handle size, border), imported before main.scss rules.</done>
</task>

<task type="auto">
  <name>Task 2: Create CropWorkspace component</name>
  <files>app/components/CropWorkspace.vue</files>
  <action>
Create components/CropWorkspace.vue implementing:

1. Container for crop area + ratio controls
2. Layout:
   - Top: image display area with CropOverlay (flex: 1, overflow: hidden)
   - Bottom: RatioControls panel (fixed height ~120px)

3. Connects to useImageStore to get selectedImage
4. Connects to useCropStore for effectiveRatio and crop state
5. Connects to useCropWindow for crop state and drag/resize methods
6. When selectedImage changes:
   - Load image into display area
   - Initialize crop window to center of image at 80% dimensions
   - If ratio preset active, constrain crop window to that ratio
7. When effectiveRatio changes:
   - Resize current crop window to maintain ratio while maximizing size
   - Keep crop window centered
8. Image display:
   - img tag with src from selectedImage.blobUrl
   - max-width: 100%, max-height: calc(100vh - 200px) for responsive
   - CropOverlay positioned absolutely over image

9. Handles:
   - "Auto crop" button (placeholder for Phase 3, shows alert "AI cropping coming in Phase 3")
   - "Export" button (placeholder for Phase 5, shows alert "Export coming in Phase 5")

Per CROP-01, CROP-02, CROP-03: CropOverlay with ratio constraints.
Per RATIO-02: EffectiveRatio watcher constrains crop window.
</action>
  <verify>
    <automated>grep -c "useImageStore\|useCropStore\|useCropWindow" app/components/CropWorkspace.vue</automated>
  </verify>
  <done>CropWorkspace component created with: image display, CropOverlay integration, RatioControls panel, ratio change handling, placeholder buttons for Phase 3/5.</done>
</task>

<task type="auto">
  <name>Task 3: Create main index.vue page layout</name>
  <files>app/pages/index.vue</files>
  <action>
Replace app/pages/index.vue with:

1. Two-column layout:
   - Left sidebar (~280px fixed): ImageList component
   - Main area (flex: 1): CropWorkspace component

2. CSS Grid layout:
   ```scss
   .app-layout {
     display: grid;
     grid-template-columns: 280px 1fr;
     height: 100vh;
     gap: 0;
   }
   ```

3. Responsive breakpoint (max-width: 768px):
   - Stack vertically: ImageList on top (~200px height), CropWorkspace below

4. Page initialization:
   - On mount: set up any global event listeners needed
   - Cleanup in onUnmounted

5. Title: "Image Cropper" in header bar (minimal, just sets context)

6. Empty state when no image selected:
   - CropWorkspace shows placeholder: "Select an image from the list to start cropping"
   - Centered text with muted color

Per UIUX-02: Responsive scaling with media query breakpoint.
Per D-01: Single-column list in sidebar layout.
</action>
  <verify>
    <automated>grep -c "app-layout\|ImageList\|CropWorkspace" app/pages/index.vue</automated>
  </verify>
  <done>index.vue replaced with: two-column layout (ImageList sidebar + CropWorkspace), responsive stacking on mobile, empty state placeholder.</done>
</task>

<task type="auto">
  <name>Task 4: Update main.scss for full dark mode and responsive layout</name>
  <files>app/assets/sass/main.scss</files>
  <action>
Update app/assets/sass/main.scss:

1. Add @use './_variables' at top (before existing rules, removes need for duplicate color vars)
2. Update body/html for full-height app:
   ```scss
   html, body, #__nuxt {
     height: 100%;
     margin: 0;
   }
   ```
3. Add .app-layout and responsive grid styles
4. Add component-specific styles:
   - .image-list: sidebar styling, scrollable
   - .crop-workspace: main area styling
   - .ratio-controls: bottom panel styling
5. Remove duplicate color definitions (now in _variables.scss)

6. Ensure dark mode properly transitions:
   - Add transition: background-color 200ms ease, color 200ms ease on body

7. Ensure form inputs use CSS variables:
   - input, button use var(--bg-secondary), var(--text-primary), var(--border)

Per UIUX-01: Dark mode support with smooth transitions.
Per UIUX-02: Responsive layout with grid.
</action>
  <verify>
    <automated>grep -c "@use.*_variables\|app-layout\|prefers-color-scheme" app/assets/sass/main.scss</automated>
  </verify>
  <done>main.scss updated with: _variables import, full-height layout, responsive grid, component styles, dark mode transitions, form input styling.</done>
</task>

<task type="auto">
  <name>Task 5: Add file import flow connecting DropZone to stores</name>
  <files>app/pages/index.vue, app/components/ImageList.vue</files>
  <action>
Update the file import flow:

1. ImageList should receive file-added events from DropZone and process them:
   - When DropZone emits 'files-added' (File[]):
     - For each File:
       - Create blob URL via useBlobRegistry
       - Get image dimensions (createImageBitmap -> naturalWidth/naturalHeight)
       - Generate thumbnail via useThumbnail (80x80)
       - Create ImageItem and add to useImageStore via addImage()
   - Auto-select first image if none selected

2. Add import handling in index.vue or create a useImportFlow composable:
   - Validates image files (type starts with 'image/')
   - Shows error toast if invalid files dropped (optional, per security)
   - Cleans up on image removal

3. Ensure ImageListItem shows loading state while thumbnail generates
   - Use v-if/v-show to handle async thumbnail loading

Per IMPT-01: File picker imports images.
Per IMPT-02: Drag-drop imports images.
Per IMPT-04: Thumbnails generated lazily.
Per PRVW-02: List item shows filename and dimensions.
</action>
  <verify>
    <automated>grep -c "addImage\|files-added\|generateThumbnail" app/pages/index.vue app/components/ImageList.vue 2>/dev/null</automated>
  </verify>
  <done>File import flow connected: DropZone -> stores -> ImageList thumbnail display. Images auto-added to store on drop/select.</done>
</task>

</tasks>

<success_criteria>
- Two-column layout renders correctly with ImageList sidebar and CropWorkspace main
- Dark mode applies correctly via CSS variables when system preference is dark
- Responsive layout stacks vertically on mobile (<768px)
- CropWorkspace shows image with CropOverlay when image is selected
- RatioControls updates crop window when preset/custom ratio selected
- File import flow works: drop/select images -> thumbnail appears in list
- All CSS uses CSS variables, no hardcoded colors
</success_criteria>

<output>
Create .planning/phases/02-core-cropping/02-03-SUMMARY.md when done
</output>
