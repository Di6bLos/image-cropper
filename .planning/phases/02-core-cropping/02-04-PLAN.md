---
phase: "02"
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - tests/composables/useFileUpload.test.ts
  - tests/composables/useThumbnail.test.ts
  - tests/composables/useCropWindow.test.ts
  - tests/stores/useImageStore.test.ts
  - tests/stores/useCropStore.test.ts
  - tests/components/DropZone.test.ts
  - tests/components/ImageListItem.test.ts
  - tests/components/CropOverlay.test.ts
autonomous: false
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-03
  - IMPT-04
  - PRVW-01
  - PRVW-02
  - PRVW-03
  - CROP-01
  - CROP-02
  - CROP-03
  - CROP-04
  - CROP-05
  - CROP-06
  - RATIO-01
  - RATIO-02
  - RATIO-03
  - RATIO-04
  - RATIO-05
  - UIUX-01
  - UIUX-02
must_haves:
  truths:
    - "All composables have passing unit tests"
    - "All stores have passing unit tests"
    - "All components have passing component tests"
    - "Human can verify the full crop flow works end-to-end"
  artifacts:
    - path: "tests/composables/useFileUpload.test.ts"
      provides: "Tests for drag tracking, file validation, add/remove/clear"
      min_lines: 60
    - path: "tests/composables/useThumbnail.test.ts"
      provides: "Tests for thumbnail generation, aspect ratio, error handling"
      min_lines: 50
    - path: "tests/composables/useCropWindow.test.ts"
      provides: "Tests for drag, resize handles, ratio constraint, bounds clamping"
      min_lines: 100
    - path: "tests/stores/useImageStore.test.ts"
      provides: "Tests for add/remove/select/clear and blob URL cleanup"
      min_lines: 50
    - path: "tests/stores/useCropStore.test.ts"
      provides: "Tests for preset ratios, custom ratio, pixel mode, mode switching"
      min_lines: 60
    - path: "tests/components/DropZone.test.ts"
      provides: "Component tests for drop zone rendering and drag events"
      min_lines: 40
    - path: "tests/components/ImageListItem.test.ts"
      provides: "Component tests for thumbnail, filename, dimensions, selection"
      min_lines: 40
    - path: "tests/components/CropOverlay.test.ts"
      provides: "Component tests for image display, crop window, handles"
      min_lines: 50
  key_links:
    - from: "tests/composables/useFileUpload.test.ts"
      to: "app/composables/useFileUpload.ts"
      via: "imports and tests"
      pattern: "useFileUpload"
    - from: "tests/composables/useThumbnail.test.ts"
      to: "app/composables/useThumbnail.ts"
      via: "imports and tests"
      pattern: "useThumbnail"
    - from: "tests/composables/useCropWindow.test.ts"
      to: "app/composables/useCropWindow.ts"
      via: "imports and tests"
      pattern: "useCropWindow"
    - from: "tests/stores/useImageStore.test.ts"
      to: "app/stores/useImageStore.ts"
      via: "imports and tests"
      pattern: "useImageStore"
    - from: "tests/stores/useCropStore.test.ts"
      to: "app/stores/useCropStore.ts"
      via: "imports and tests"
      pattern: "useCropStore"
    - from: "tests/components/DropZone.test.ts"
      to: "app/components/DropZone.vue"
      via: "mounts and tests"
      pattern: "DropZone"
    - from: "tests/components/ImageListItem.test.ts"
      to: "app/components/ImageListItem.vue"
      via: "mounts and tests"
      pattern: "ImageListItem"
    - from: "tests/components/CropOverlay.test.ts"
      to: "app/components/CropOverlay.vue"
      via: "mounts and tests"
      pattern: "CropOverlay"
---

<objective>
Write unit and component tests for all Phase 2 functionality, verifying core cropping behavior.
</objective>

<context>
@.planning/phases/02-core-cropping/02-01-PLAN.md
@.planning/phases/02-core-cropping/02-02-PLAN.md
@.planning/phases/02-core-cropping/02-03-PLAN.md
@tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write useFileUpload tests</name>
  <files>tests/composables/useFileUpload.test.ts</files>
  <action>
Create tests/composables/useFileUpload.test.ts:

1. Test counter-based drag tracking:
   - Simulate dragenter -> isDragging true
   - Simulate dragleave (counter decrement) -> isDragging false when counter=0
   - Simulate nested dragleave -> isDragging stays true

2. Test file validation:
   - Non-image files filtered out
   - Files over maxSize filtered out
   - Files beyond maxFiles filtered out

3. Test addFiles:
   - Adds valid files to files array
   - Ignores invalid files

4. Test removeFile and clearFiles

5. Test openFilePicker (mock input click)

Use mock File objects, simulate drag events.
</action>
  <verify>
    <automated>vitest run tests/composables/useFileUpload.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>useFileUpload tests created covering drag tracking, file validation, add/remove/clear operations.</done>
</task>

<task type="auto">
  <name>Task 2: Write useThumbnail tests</name>
  <files>tests/composables/useThumbnail.test.ts</files>
  <action>
Create tests/composables/useThumbnail.test.ts:

1. Mock createImageBitmap and canvas context
2. Test thumbnail generation:
   - Generates blob URL
   - Maintains aspect ratio
   - Centers in square canvas
   - Respects size parameter
   - Handles devicePixelRatio

3. Test error handling:
   - Returns null on bitmap creation failure
   - Returns null on canvas context failure

Mock useCanvasPool and useBlobRegistry.
</action>
  <verify>
    <automated>vitest run tests/composables/useThumbnail.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>useThumbnail tests created covering aspect ratio, centering, size handling, error cases.</done>
</task>

<task type="auto">
  <name>Task 3: Write useCropWindow tests</name>
  <files>tests/composables/useCropWindow.test.ts</files>
  <action>
Create tests/composables/useCropWindow.test.ts:

1. Test initial state (centered, full image)

2. Test drag:
   - startDrag records initial position
   - onMouseMove updates x/y
   - onMouseUp resets isDragging
   - Position clamped to bounds

3. Test SE resize (no position adjustment needed):
   - startResize with 'se'
   - onMouseMove with positive dx/dy increases width/height
   - Aspect ratio maintained when ratio set
   - onMouseUp resets state

4. Test NW resize (adjusts x AND y):
   - Positive delta -> width/height decrease, x/y increase
   - Ratio maintained
   - Clamped to bounds

5. Test NE resize (adjusts y only):
   - Positive dy -> height decreases, y increases
   - Ratio maintained

6. Test SW resize (adjusts x only):
   - Positive dx -> width decreases, x increases
   - Ratio maintained

7. Test ratio constraint:
   - When ratio set, width/height always maintain ratio
   - Resize from different handles maintains opposite corner position

8. Test bounds clamping:
   - x never goes below 0
   - y never goes below 0
   - x + width never exceeds imageWidth
   - y + height never exceeds imageHeight

Use mock mouse events.
</action>
  <verify>
    <automated>vitest run tests/composables/useCropWindow.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>useCropWindow tests created covering drag, all 4 handle resizes, aspect ratio constraint, bounds clamping.</done>
</task>

<task type="auto">
  <name>Task 4: Write useImageStore tests</name>
  <files>tests/stores/useImageStore.test.ts</files>
  <action>
Create tests/stores/useImageStore.test.ts:

1. Test addImage:
   - Adds ImageItem to images array
   - Returns correct selectedImage

2. Test removeImage:
   - Removes image by id
   - Revokes blob URLs
   - If removed was selected, clears selectedId

3. Test selectImage:
   - Sets selectedId
   - selectedImage computed updates

4. Test clearAll:
   - Revokes all blob URLs
   - Clears images array and selectedId

5. Test readonly state (should not be directly modifiable)

Mock useBlobRegistry.
</action>
  <verify>
    <automated>vitest run tests/stores/useImageStore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>useImageStore tests created covering add/remove/select/clear operations and blob URL cleanup.</done>
</task>

<task type="auto">
  <name>Task 5: Write useCropStore tests</name>
  <files>tests/stores/useCropStore.test.ts</files>
  <action>
Create tests/stores/useCropStore.test.ts:

1. Test effectiveRatio in ratio mode:
   - 16:9 -> 1.777...
   - 3:2 -> 1.5
   - 1:1 -> 1
   - 2:3 -> 0.666...

2. Test effectiveRatio with custom:
   - customWidth=7, customHeight=4 -> 1.75

3. Test effectiveRatio in pixel mode:
   - Returns pixelWidth / pixelHeight

4. Test setMode:
   - Switches between 'ratio' and 'pixel'
   - effectiveRatio recomputes

5. Test setPreset:
   - Updates preset
   - effectiveRatio updates

6. Test setCustomRatio:
   - Updates customWidth/customHeight
   - effectiveRatio updates if preset='custom'

7. Test setPixelSize:
   - Updates pixelWidth/pixelHeight
   - effectiveRatio updates if mode='pixel'
</action>
  <verify>
    <automated>vitest run tests/stores/useCropStore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>useCropStore tests created covering all preset ratios, custom ratio, pixel mode, mode switching.</done>
</task>

<task type="auto">
  <name>Task 6: Write DropZone component test</name>
  <files>tests/components/DropZone.test.ts</files>
  <action>
Create tests/components/DropZone.test.ts:

1. Test renders drop zone element
2. Test drag-over class applied on dragenter
3. Test drag-over class removed on dragleave (counter=0)
4. Test file input triggered on button click
5. Test emits 'files-added' on drop

Use @vue/test-utils mount with simulate drag events.
</action>
  <verify>
    <automated>vitest run tests/components/DropZone.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>DropZone component tests created.</done>
</task>

<task type="auto">
  <name>Task 7: Write ImageListItem component test</name>
  <files>tests/components/ImageListItem.test.ts</files>
  <action>
Create tests/components/ImageListItem.test.ts:

1. Test renders thumbnail img with correct src
2. Test renders filename text
3. Test renders dimensions text
4. Test has 'is-selected' class when isSelected=true
5. Test emits 'select' event with id on click

Use @vue/test-utils.
</action>
  <verify>
    <automated>vitest run tests/components/ImageListItem.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>ImageListItem component tests created.</done>
</task>

<task type="auto">
  <name>Task 8: Write CropOverlay component test</name>
  <files>tests/components/CropOverlay.test.ts</files>
  <action>
Create tests/components/CropOverlay.test.ts:

1. Test renders img element with correct src
2. Test renders crop window at correct position
3. Test renders 4 handles
4. Test has white border on crop window
5. Test mask covers area outside crop window (via clip-path)

Use @vue/test-utils and check computed styles.
</action>
  <verify>
    <automated>vitest run tests/components/CropOverlay.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>CropOverlay component tests created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Manual verification of full crop flow</name>
  <action>Verify the full crop flow manually:</action>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. Drag an image onto the DropZone - verify it appears in ImageList
3. Click the image in the list - verify CropOverlay appears
4. Drag the crop window - verify it moves
5. Click corner handles - verify resize with aspect ratio maintained
6. Click ratio preset buttons - verify crop window constrains to ratio
7. Enter custom ratio - verify crop window updates
8. Switch to Pixel mode - verify inputs change
9. Toggle system dark mode - verify colors update
10. Resize browser window - verify responsive layout

Report any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<success_criteria>
- All composable tests pass (5 test files)
- All store tests pass (2 test files)
- All component tests pass (3 test files)
- Manual verification shows full crop flow working
- No console errors during manual testing
</success_criteria>

<output>
Create .planning/phases/02-core-cropping/02-04-SUMMARY.md when done
</output>
