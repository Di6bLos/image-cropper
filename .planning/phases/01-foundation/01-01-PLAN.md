---
phase: "01-foundation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - nuxt.config.ts
  - tsconfig.json
  - app/app.vue
  - app/pages/index.vue
  - app/stores/useAppStore.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Nuxt 3 project builds successfully with Pinia, Sass, and nuxt-security modules"
    - "COOP/COEP headers are served on all routes for SharedArrayBuffer support"
    - "TypeScript compiles with loose/standard configuration (D-01)"
    - "Pinia state management is accessible in Vue components via useAppStore"
  artifacts:
    - path: "package.json"
      provides: "npm dependencies including nuxt, @pinia/nuxt, sass, nuxt-security"
      contains: "@pinia/nuxt"
    - path: "nuxt.config.ts"
      provides: "COOP/COEP headers via nuxt-security (D-03), module registrations"
      contains: "crossOriginEmbedderPolicy"
    - path: "tsconfig.json"
      provides: "Loose TypeScript configuration (D-01)"
      contains: "strict"
    - path: "app/pages/index.vue"
      provides: "Entry page confirming SSR and hydration work"
      exports: ["default"]
    - path: "app/stores/useAppStore.ts"
      provides: "Pinia store accessible in Vue components"
      exports: ["useAppStore"]
  key_links:
    - from: "nuxt.config.ts"
      to: "package.json"
      via: "module registration"
      pattern: "@pinia/nuxt"
    - from: "app/pages/index.vue"
      to: "app/stores/useAppStore.ts"
      via: "useStore composable"
      pattern: "useAppStore"
---

<objective>
Initialize Nuxt 3 project with Pinia, Sass, and nuxt-security module. Configure COOP/COEP headers for cross-origin isolation (D-03). Set up loose TypeScript configuration (D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md

## Key Technical References

### nuxt-security Configuration (D-03, RESEARCH.md)
```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['nuxt-security'],
  security: {
    headers: {
      crossOriginEmbedderPolicy: 'require-corp',
      crossOriginOpenerPolicy: 'same-origin',
    },
  },
})
```

### @pinia/nuxt Registration (RESEARCH.md)
```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@pinia/nuxt'],
})
```

### Loose TypeScript Config (D-01, RESEARCH.md)
tsconfig.json should have `strict: false` or omit strict fields for loose mode.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Nuxt 3 project with dependencies</name>
  <files>package.json, tsconfig.json, nuxt.config.ts</files>
  <read_first>package.json (will be created fresh)</read_first>
  <action>
    Initialize a new Nuxt 3 project in the current directory using `npx nuxi init . --force --packageManager npm`.

    After initialization, install additional dependencies:
    - npm install @pinia/nuxt sass nuxt-security

    Update package.json scripts to include:
    - "build": "nuxt build"
    - "dev": "nuxt dev"
    - "generate": "nuxt generate"
    - "preview": "nuxt preview"

    Create tsconfig.json with loose TypeScript settings:
    - Set compilerOptions.strict to false OR omit strict-related fields entirely
    - Include path aliases: ~/* and @/* pointing to ./
    - Extend from "./.nuxt/tsconfig.json"

    Create nuxt.config.ts with:
    - modules: ['@pinia/nuxt', 'nuxt-security']
    - security.headers with crossOriginEmbedderPolicy: 'require-corp' and crossOriginOpenerPolicy: 'same-origin' (per D-03)
    - css: ['~/assets/sass/main.scss']
    - devtools: { enabled: true }
  </action>
  <acceptance_criteria>
    - package.json exists with nuxt, @pinia/nuxt, sass, nuxt-security in dependencies
    - nuxt.config.ts has modules: ['@pinia/nuxt', 'nuxt-security']
    - nuxt.config.ts has security.headers.crossOriginEmbedderPolicy: 'require-corp'
    - nuxt.config.ts has security.headers.crossOriginOpenerPolicy: 'same-origin'
    - tsconfig.json has strict: false or no strict fields
    - tsconfig.json extends .nuxt/tsconfig.json
  </acceptance_criteria>
  <verify>
    <automated>npm install && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Nuxt 3 project builds successfully with all modules and headers configured</done>
</task>

<task type="auto">
  <name>Task 2: Create Sass assets, app entry point, and Pinia store</name>
  <files>app/assets/sass/main.scss, app/app.vue, app/pages/index.vue, app/stores/useAppStore.ts</files>
  <read_first>app/app.vue (will be created fresh)</read_first>
  <action>
    Create directory app/assets/sass/.

    Create app/assets/sass/main.scss with:
    - CSS custom properties for dark mode support (--bg-primary, --bg-secondary, --text-primary, --text-secondary, --accent)
    - Base reset styles (box-sizing: border-box, margin: 0, padding: 0)
    - Body styles with font-family and background-color using CSS variables
    - Media query for prefers-color-scheme to toggle dark/light mode

    Create app/app.vue as a simple wrapper:
    - <NuxtPage /> component
    - Minimal styling scoped to html/body if needed

    Create app/pages/index.vue as the entry page:
    - Basic template with <h1>Welcome to Image Cropper</h1>
    - Import and call useAppStore to verify Pinia integration works
    - Add a simple reactive reference (e.g., const count = ref(0)) to verify SSR and hydration
    - Call useAppStore() in script setup to demonstrate store accessibility

    Create app/stores/useAppStore.ts as a minimal Pinia store:
    - Define a simple state with a message or counter
    - Export defineStore('app', ...) with setup store syntax
    - Include at least one state property and one action
    - This verifies the Pinia truth is verifiable
  </action>
  <acceptance_criteria>
    - app/assets/sass/main.scss exists with CSS variables for dark mode
    - app/assets/sass/main.scss has body styles and dark mode media query
    - app/app.vue contains <NuxtPage />
    - app/pages/index.vue renders without errors
    - app/pages/index.vue imports and uses useAppStore
    - app/stores/useAppStore.ts exists and exports a Pinia store
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(ERROR|error|warn)" | head -10</automated>
  </verify>
  <done>Sass assets, app entry points, and Pinia store created; project structure established</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | Nuxt SSR renders pages; no untrusted input in Phase 1 infrastructure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm packages | mitigate | Packages verified in RESEARCH.md package audit |
| T-01-02 | Information Disclosure | COOP/COEP headers | accept | Headers are intentional; enforce isolation |
</threat_model>

<verification>
1. Run `npm install` - no errors
2. Run `npm run build` - exits with code 0
3. Check build output for .output/server/index.js containing the security headers
4. Verify COOP/COEP headers are present in nuxt.config.ts
5. Verify app/stores/useAppStore.ts exists and is imported in index.vue
</verification>

<success_criteria>
- npm install completes without errors
- npm run build produces .output directory with server and client builds
- nuxt.config.ts contains both crossOriginEmbedderPolicy and crossOriginOpenerPolicy in security.headers
- TypeScript compiles with loose settings (no strict null errors)
- app/pages/index.vue renders successfully
- app/stores/useAppStore.ts exists and is used in index.vue
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-foundation/01-01-SUMMARY.md when done
</output>
